.nested-fields
  = f.inputs do
    = f.input :budget_post, {:include_blank => false}
    = f.input :description
    = f.input :amount
    %li
      %label
      = link_to_remove_association "Remove item", f, :class => "btn danger"
