- title index_title(Vendor)

%h2
  = index_title(Vendor)
  = link_to image_tag("icons/add.png", :alt => "New vendor"), new_vendor_path

= render "list", :locals => { :vendors => @vendors }

= paginate @vendors
